<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <h1>URL</h1>
    <% for (var i in urls) { %>
    <li><%= urls[i] %></li>
    <% }; %>

    <h1>Shortened URL</h1>
    <p>
    <% for (var i in urls) { %>
    <li><%= i %>  %></li>
    <% }; %>
    </p>
</body>
</html>